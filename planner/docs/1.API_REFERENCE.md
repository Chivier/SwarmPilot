# Planner API Reference

## Overview

The Planner service optimizes model deployment across instances using configurable algorithms (simulated annealing or integer programming). It integrates with PyLet for cluster management to deploy, scale, and migrate model instances.

**Key Capabilities:**
- Optimal model-to-instance assignment with configurable algorithms
- Constraint-aware planning (batch capacity, change limits)
- Multi-objective optimization functions
- PyLet-based cluster management for deployment execution
- Instance registration and lifecycle management


## Data Models

### PlannerInput

Optimization parameters and algorithm configuration.

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| **Core Parameters** ||||
| `M` | `int` | *required* | Number of instances (> 0) |
| `N` | `int` | *required* | Number of model types (> 0) |
| `B` | `List[List[float]]` | *required* | Batch capacity matrix [M×N] |
| `initial` | `List[int]` | *required* | Initial deployment [M], -1 = no model |
| `a` | `float` | *required* | Change constraint (0 < a ≤ 1) |
| `target` | `List[float]` | *required* | Target request distribution [N] |
| **Algorithm** ||||
| `algorithm` | `str` | `"simulated_annealing"` | `"simulated_annealing"` or `"integer_programming"` |
| `objective_method` | `str` | `"relative_error"` | `"relative_error"`, `"ratio_difference"`, or `"weighted_squared"` |
| `verbose` | `bool` | `true` | Enable logging |
| **Simulated Annealing** ||||
| `initial_temp` | `float` | `100.0` | Starting temperature |
| `final_temp` | `float` | `0.01` | Ending temperature |
| `cooling_rate` | `float` | `0.95` | Temperature decay |
| `max_iterations` | `int` | `5000` | Max iterations |
| `iterations_per_temp` | `int` | `100` | Iterations per temperature |
| **Integer Programming** ||||
| `solver_name` | `str` | `"PULP_CBC_CMD"` | Solver backend |
| `time_limit` | `int` | `300` | Timeout (seconds) |

### PlannerOutput

| Field | Type | Description |
|-------|------|-------------|
| `deployment` | `List[int]` | Optimized assignment [M] (model IDs) |
| `score` | `float` | Objective value (lower = better) |
| `stats` | `dict` | Algorithm statistics |
| `service_capacity` | `List[float]` | Capacity per model [N] |
| `changes_count` | `int` | Changes from initial state |

### InstanceInfo

| Field | Type | Description |
|-------|------|-------------|
| `endpoint` | `str` | Instance API endpoint |
| `current_model` | `str` | Current model name |

---

## Core API Endpoints

### `GET /health`

Health check endpoint for monitoring and load balancing.

**Response:**
```json
{
  "status": "healthy",
  "timestamp": "2025-01-07T12:00:00Z"
}
```

### `GET /info`

Get service information and available algorithms.

**Response:**
```json
{
  "service": "planner",
  "version": "0.1.0",
  "algorithms": ["simulated_annealing", "integer_programming"],
  "objective_methods": ["relative_error", "ratio_difference", "weighted_squared"]
}
```

### `POST /plan`

Compute optimal deployment plan (no execution).

**Request:** `PlannerInput` | **Response:** `PlannerOutput`

<details>
<summary><b>Example</b></summary>

```bash
curl -X POST http://localhost:8000/plan \
  -H "Content-Type: application/json" \
  -d '{
    "M": 4, "N": 3,
    "B": [[10, 5, 0], [8, 6, 4], [0, 10, 8], [6, 0, 12]],
    "initial": [0, 1, 2, 2],
    "a": 0.5,
    "target": [20, 30, 25]
  }'
```

```json
{
  "deployment": [0, 1, 1, 2],
  "score": 0.0667,
  "stats": {"algorithm": "simulated_annealing", "iterations": 5000, "acceptance_rate": 0.247},
  "service_capacity": [10.0, 16.0, 12.0],
  "changes_count": 1
}
```
</details>

### `POST /instance/register`

Register an instance with the planner for tracking.

**Request:**
```json
{
  "instance_id": "inst-123",
  "model_id": "model_a",
  "endpoint": "http://instance:8080",
  "platform_info": {
    "software_name": "vllm",
    "software_version": "0.3.0",
    "hardware_name": "nvidia-a100"
  }
}
```

**Response:**
```json
{
  "success": true,
  "message": "Instance inst-123 registered successfully for model model_a"
}
```

---

## PyLet API Endpoints

PyLet endpoints provide cluster management for deploying and managing model instances.

### `GET /pylet/status`

Get PyLet service status and active instances.

**Response:**
```json
{
  "enabled": true,
  "initialized": true,
  "current_state": {"model-a": 2, "model-b": 1},
  "total_instances": 3,
  "active_instances": [
    {
      "pylet_id": "p1",
      "instance_id": "i1",
      "model_id": "model-a",
      "endpoint": "http://localhost:8001",
      "status": "active",
      "error": null
    }
  ]
}
```

### `POST /pylet/deploy`

Deploy instances to target state via PyLet.

**Request:**
```json
{
  "target_state": {"model-a": 2, "model-b": 1},
  "wait_for_ready": true,
  "register_with_scheduler": true
}
```

**Response:**
```json
{
  "success": true,
  "added_count": 2,
  "removed_count": 0,
  "active_instances": [...],
  "failed_adds": [],
  "failed_removes": [],
  "error": null
}
```

### `POST /pylet/scale`

Scale a specific model to target count.

**Request:**
```json
{
  "model_id": "model-a",
  "target_count": 3,
  "wait_for_ready": true
}
```

**Response:**
```json
{
  "success": true,
  "model_id": "model-a",
  "previous_count": 2,
  "current_count": 3,
  "added": 1,
  "removed": 0,
  "active_instances": [...],
  "error": null
}
```

### `POST /pylet/migrate`

Migrate an instance to a different model.

**Request:**
```json
{
  "pylet_id": "p1",
  "target_model_id": "model-b"
}
```

**Response:**
```json
{
  "success": true,
  "old_pylet_id": "p1",
  "new_pylet_id": "p2",
  "model_id": "model-b",
  "endpoint": "http://localhost:8002",
  "error": null
}
```

### `POST /pylet/optimize`

Run optimizer and deploy result via PyLet.

**Request:**
```json
{
  "target": [0.5, 0.3, 0.2],
  "model_ids": ["model-a", "model-b", "model-c"],
  "B": [[1.0, 0.8, 0.6], [0.9, 1.0, 0.7]],
  "a": 0.3,
  "objective_method": "ratio_difference",
  "algorithm": "simulated_annealing",
  "wait_for_ready": true
}
```

**Response:**
```json
{
  "deployment": [0, 1],
  "objective_value": 0.05,
  "service_capacity": [1.0, 1.0, 0.0],
  "changes": 1,
  "optimization_stats": {...},
  "deployment_success": true,
  "added_count": 2,
  "removed_count": 0,
  "active_instances": [...],
  "error": null
}
```

### `POST /pylet/terminate-all`

Terminate all PyLet-managed instances.

**Query Parameters:**
- `wait_for_drain` (bool, default: false): Wait for instances to drain before termination

**Response:**
```json
{
  "success": true,
  "total": 3,
  "succeeded": 3,
  "failed": 0,
  "details": {"p1": true, "p2": true, "p3": true}
}
```

---

## Scheduler Compatibility Endpoints

These endpoints provide compatibility with the scheduler interface for instance lifecycle management.

### `POST /instance/drain`

Signal an instance to start draining (stop accepting new tasks).

### `GET /instance/drain/status`

Check if an instance can be safely removed.

### `POST /instance/remove`

Remove an instance from tracking.

### `POST /task/resubmit`

Resubmit a task from a failed instance.

---

## Algorithm Selection

| Algorithm | Best For | Trade-off | Key Parameters |
|-----------|----------|-----------|----------------|
| **Simulated Annealing** | Large search spaces, fast approximation | Speed ↔ Optimality | Temperature schedule, iterations |
| **Integer Programming** | Guaranteed optimal, smaller problems | Optimality ↔ Time | Solver, time limit |

## Objective Functions

| Method | Formula Focus | Use Case |
|--------|---------------|----------|
| `relative_error` | Relative deviation | Balanced general optimization |
| `ratio_difference` | Ratio mismatch | Proportional fairness |
| `weighted_squared` | Squared errors | Penalize large deviations |

---

## Environment Variables

### Basic Configuration

| Variable | Type | Default | Description |
|----------|------|---------|-------------|
| `PLANNER_HOST` | `str` | `"0.0.0.0"` | Server bind address |
| `PLANNER_PORT` | `int` | `8000` | Server port |
| `SCHEDULER_URL` | `str` | `None` | Scheduler service URL for instance registration |
| `INSTANCE_TIMEOUT` | `int` | `30` | Instance deployment timeout (seconds) |
| `INSTANCE_MAX_RETRIES` | `int` | `3` | Max retry attempts for deployment |
| `INSTANCE_RETRY_DELAY` | `float` | `1.0` | Delay between retries (seconds) |

### PyLet Configuration

| Variable | Type | Default | Description |
|----------|------|---------|-------------|
| `PYLET_ENABLED` | `bool` | `false` | Enable PyLet integration |
| `PYLET_HEAD_URL` | `str` | `None` | PyLet head service URL (required if enabled) |
| `PYLET_BACKEND` | `str` | `"vllm"` | Default backend (`vllm` or `sglang`) |
| `PYLET_GPU_COUNT` | `int` | `1` | Default GPU count per instance |
| `PYLET_DEPLOY_TIMEOUT` | `float` | `300.0` | Deployment timeout (seconds) |
| `PYLET_DRAIN_TIMEOUT` | `float` | `60.0` | Drain timeout (seconds) |

**Example:**
```bash
# Enable PyLet with vLLM backend
export PYLET_ENABLED=true
export PYLET_HEAD_URL=http://pylet-head:8000
export PYLET_BACKEND=vllm
export PYLET_GPU_COUNT=1
```

# Instance Redeployment API

This document describes the instance redeployment mechanism, which enables zero-downtime model updates and maintenance by gracefully extracting pending tasks and redistributing them to other instances.

> **See Also**: [Instance Management API](3.INSTANCE_API.md) for general instance operations

---

## Overview

The redeployment mechanism allows instances to be updated (new model version, configuration changes, maintenance) without losing queued tasks. This is achieved through:

1. **Task Extraction**: Pending tasks are extracted from the redeploying instance
2. **Task Redistribution**: Extracted tasks are redistributed to other active instances
3. **Priority Preservation**: Task priority (enqueue_time) is maintained during redistribution
4. **Status Management**: Instance transitions through ACTIVE → REDEPLOYING → ACTIVE states

**Key Features:**
- No task loss during redeployment
- Priority queue ordering preserved
- Running task completes normally
- Automatic task redistribution using current scheduling strategy
- Comprehensive statistics and error reporting

---

## Redeployment Workflow

```
┌─────────────────────────────────────────────────────────────┐
│                    Redeployment Workflow                     │
└─────────────────────────────────────────────────────────────┘

1. Client initiates redeployment
   POST /instance/redeploy/start
   ↓
2. Scheduler marks instance as REDEPLOYING
   (prevents new task assignments)
   ↓
3. Scheduler requests task extraction from instance
   HTTP POST to instance's /redeploy/start
   ↓
4. Instance extracts pending tasks from queue
   (running task continues execution)
   ↓
5. Instance returns pending tasks to scheduler
   ↓
6. Scheduler redistributes tasks to other instances
   (preserves enqueue_time for priority)
   ↓
7. Instance completes current task (if any)
   ↓
8. Instance updates model/configuration
   ↓
9. Instance calls POST /redeploy/complete (local)
   ↓
10. Instance notifies scheduler
    POST /instance/redeploy/complete
    ↓
11. Scheduler updates instance to ACTIVE status
    ↓
12. Instance ready to accept new tasks ✓
```

---

## API Endpoints

### POST `/instance/redeploy/start`

Initiate instance redeployment and redistribute pending tasks.

**Request Body:**
```json
{
  "instance_id": "instance-gpu-1",
  "redeploy_reason": "Updating to model v2.0",
  "target_model_id": "easyocr/dec-v2"
}
```

**Field Descriptions:**
- `instance_id` (required): ID of the instance to redeploy
- `redeploy_reason` (optional): Human-readable reason for redeployment
- `target_model_id` (optional): New model ID after redeployment (for documentation)

**Success Response (200 OK):**
```json
{
  "success": true,
  "message": "Instance instance-gpu-1 is now redeploying. Returned 5 pending tasks. Successfully redistributed 5, failed 0. Current task task-123 is still running.",
  "returned_tasks": [
    {
      "task_id": "task-100",
      "model_id": "easyocr/dec",
      "task_input": {"image": "base64..."},
      "enqueue_time": 1699876543.123,
      "metadata": {}
    },
    {
      "task_id": "task-101",
      "model_id": "easyocr/dec",
      "task_input": {"image": "base64..."},
      "enqueue_time": 1699876544.456,
      "metadata": {}
    }
  ],
  "redistributed_tasks": ["task-100", "task-101", "task-102", "task-103", "task-104"],
  "failed_redistributions": [],
  "current_task": {
    "task_id": "task-123",
    "estimated_completion_ms": 2500
  },
  "estimated_redeploy_time_ms": 2500
}
```

**Response Fields:**
- `success`: Whether redeployment was initiated successfully
- `message`: Human-readable status message
- `returned_tasks`: Full task data for all extracted pending tasks
- `redistributed_tasks`: List of task IDs successfully redistributed
- `failed_redistributions`: List of task IDs that failed to redistribute
- `current_task`: Information about currently running task (if any)
- `estimated_redeploy_time_ms`: Estimated time until instance is ready

**Error Responses:**

**404 Not Found** - Instance doesn't exist:
```json
{
  "detail": {
    "success": false,
    "error": "Instance not found"
  }
}
```

**400 Bad Request** - Instance not in ACTIVE state:
```json
{
  "detail": {
    "success": false,
    "error": "Instance must be in ACTIVE state, current state: DRAINING"
  }
}
```

**500 Internal Server Error** - Communication failure:
```json
{
  "detail": {
    "success": false,
    "error": "Failed to communicate with instance: Connection refused"
  }
}
```

**Usage Example:**
```bash
curl -X POST http://localhost:8000/instance/redeploy/start \
  -H "Content-Type: application/json" \
  -d '{
    "instance_id": "instance-gpu-1",
    "redeploy_reason": "Security update",
    "target_model_id": "easyocr/dec-v2.1"
  }'
```

---

### POST `/instance/redeploy/complete`

Mark instance redeployment as complete and return to ACTIVE status.

This endpoint should be called after the instance has finished updating its model/configuration and is ready to accept new tasks.

**Request Body:**
```json
{
  "instance_id": "instance-gpu-1",
  "model_id": "easyocr/dec-v2",
  "endpoint": "http://192.168.1.100:8300",
  "platform_info": {
    "software_name": "docker",
    "software_version": "24.0",
    "hardware_name": "nvidia-rtx-3090"
  }
}
```

**Field Descriptions:**
- Same as registration, but may contain updated values
- `model_id` may be different from original (model upgrade)
- `platform_info` may be updated (software version upgrade)
- `endpoint` typically remains the same

**Success Response (200 OK):**
```json
{
  "success": true,
  "message": "Instance redeployment completed successfully. Instance is now ACTIVE.",
  "instance": {
    "instance_id": "instance-gpu-1",
    "model_id": "easyocr/dec-v2",
    "endpoint": "http://192.168.1.100:8300",
    "status": "active",
    "platform_info": {
      "software_name": "docker",
      "software_version": "24.0",
      "hardware_name": "nvidia-rtx-3090"
    },
    "registered_at": "2024-11-11T12:00:00Z"
  }
}
```

**Error Responses:**

**404 Not Found** - Instance doesn't exist:
```json
{
  "detail": {
    "success": false,
    "error": "Instance not found"
  }
}
```

**400 Bad Request** - Instance not in REDEPLOYING state:
```json
{
  "detail": {
    "success": false,
    "error": "Instance must be in REDEPLOYING state, current state: ACTIVE"
  }
}
```

**Usage Example:**
```bash
curl -X POST http://localhost:8000/instance/redeploy/complete \
  -H "Content-Type: application/json" \
  -d '{
    "instance_id": "instance-gpu-1",
    "model_id": "easyocr/dec-v2",
    "endpoint": "http://192.168.1.100:8300",
    "platform_info": {
      "software_name": "docker",
      "software_version": "24.0",
      "hardware_name": "nvidia-rtx-3090"
    }
  }'
```

---

## Task Priority Preservation

The redeployment mechanism preserves task priority using the `enqueue_time` field:

### How It Works

1. **Original Enqueue**: When a task is submitted, it receives an `enqueue_time` (Unix timestamp)
2. **Priority Queue**: Tasks are ordered by `enqueue_time` (earlier time = higher priority)
3. **Extraction**: During redeployment, tasks are extracted with their original `enqueue_time`
4. **Redistribution**: Tasks are submitted to new instances with preserved `enqueue_time`
5. **Queue Insertion**: New instance inserts task at correct position based on `enqueue_time`

### Example

```
Instance A queue before redeployment:
  task-1 (enqueue_time: 1000.0)  ← oldest, highest priority
  task-2 (enqueue_time: 1100.0)
  task-3 (enqueue_time: 1200.0)  ← newest, lowest priority

After redistribution to Instance B:
Instance B existing queue:
  task-10 (enqueue_time: 1050.0)
  task-11 (enqueue_time: 1150.0)
  task-12 (enqueue_time: 1250.0)

Instance B queue after insertion:
  task-1  (enqueue_time: 1000.0)  ← inserted at front
  task-10 (enqueue_time: 1050.0)
  task-2  (enqueue_time: 1100.0)  ← inserted in middle
  task-11 (enqueue_time: 1150.0)
  task-3  (enqueue_time: 1200.0)  ← inserted in middle
  task-12 (enqueue_time: 1250.0)
```

**Benefits:**
- Tasks don't lose their place in the global queue
- Fair processing order maintained across instances
- FIFO behavior preserved when `enqueue_time` not specified

---

## Task Redistribution Strategy

Redistributed tasks use the scheduler's current scheduling strategy:

### Strategy Behavior

1. **Round Robin**: Tasks distributed evenly across available instances
2. **Shortest Queue**: Tasks assigned to instance with shortest expected completion time
3. **Probabilistic**: Tasks assigned based on predicted quantiles

### Instance Exclusion

The redeploying instance is automatically excluded from task redistribution:
- Prevents tasks from being assigned back to the instance being updated
- Ensures tasks are only assigned to ACTIVE instances
- Redistributes to instances with same `model_id`

### Failure Handling

If task redistribution fails:
- Individual failure is logged with task ID
- Failed task IDs returned in `failed_redistributions` array
- Successfully redistributed tasks continue processing
- Manual intervention may be required for failed tasks

---

## Best Practices

### Pre-Redeployment Checks

```bash
# 1. Check instance status
curl http://localhost:8000/instance/info?instance_id=instance-gpu-1

# 2. Ensure multiple instances available for task redistribution
curl http://localhost:8000/instance/list?model_id=easyocr/dec
# Should return at least 2 instances

# 3. Check current queue size
curl http://localhost:8000/instance/info?instance_id=instance-gpu-1
# Review pending_tasks count
```

### During Redeployment

```bash
# Monitor redistribution progress
# Check returned_tasks, redistributed_tasks, and failed_redistributions
curl -X POST http://localhost:8000/instance/redeploy/start \
  -H "Content-Type: application/json" \
  -d '{"instance_id": "instance-gpu-1"}'

# If current_task exists, wait for completion
# estimated_redeploy_time_ms provides guidance
```

### Post-Redeployment

```bash
# 1. Verify instance is ACTIVE
curl http://localhost:8000/instance/info?instance_id=instance-gpu-1

# 2. Verify queue is accepting tasks
# Submit a test task to the redeployed instance

# 3. Monitor for errors
# Check logs for any redistribution failures
```

### Error Recovery

**If redistribution fails:**
```bash
# 1. Check failed_redistributions array in response
# 2. Manually resubmit failed tasks if needed
# 3. Check instance logs for detailed error messages
```

**If instance doesn't complete:**
```bash
# 1. Check instance's /redeploy/status endpoint
# 2. Verify current task completed
# 3. Manually call /instance/redeploy/complete if needed
```

---

## Monitoring and Metrics

### Key Metrics to Track

1. **Redistribution Success Rate**:
   ```
   success_rate = len(redistributed_tasks) / len(returned_tasks)
   ```

2. **Redeployment Duration**:
   - Time from `/redeploy/start` to `/redeploy/complete`
   - Should approximate `estimated_redeploy_time_ms`

3. **Task Count**:
   - `len(returned_tasks)`: How many tasks were pending
   - `len(failed_redistributions)`: How many failed to redistribute

### Logging

All redeployment operations are logged with:
- Instance ID
- Number of tasks extracted/redistributed
- Failure reasons
- Status transitions

**Example Log Output:**
```
INFO: Starting redeployment for instance instance-gpu-1
INFO: Instance instance-gpu-1 returned 5 pending tasks, current_task: task-123
INFO: Redistributing 5 tasks from instance-gpu-1
INFO: Task task-100: Successfully redistributed to instance-gpu-2
INFO: Completed redeployment for instance instance-gpu-1. New model: easyocr/dec-v2, Status: ACTIVE
```

---

## Limitations and Considerations

### Current Limitations

1. **Single Instance Risk**: If only one instance exists for a model, redeployment will fail (no redistribution target)
2. **Running Task**: Currently executing task must complete before model update
3. **Status Requirement**: Only ACTIVE instances can be redeployed
4. **Manual Recovery**: Failed redistributions require manual intervention

### Capacity Planning

- Always maintain N+1 instances for mission-critical models
- Plan redeployments during low-traffic periods when possible
- Monitor queue sizes before initiating redeployment

### Future Enhancements

- Automatic retry for failed redistributions
- Batch redeployment of multiple instances
- Gradual traffic shifting during redeployment
- Health checks before marking instance as ACTIVE

---

## Related Documentation

- [Instance Management API](3.INSTANCE_API.md) - General instance operations
- [Task API](4.TASK_API.md) - Task submission and management
- [Scheduling Strategies](8.SCHEDULING_STRATEGIES.md) - How tasks are distributed
- [Background Scheduler](10.BACKGROUND_SCHEDULER.md) - Async task scheduling

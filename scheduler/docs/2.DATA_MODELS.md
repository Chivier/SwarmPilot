# Data Models

This document describes all data models used by the scheduler service for API requests, responses, and internal state management.

---

## Core Models

### Task

Represents a computational task to be executed.

| Field | Type | Description |
|-------|------|-------------|
| `task_id` | `str` | Unique identifier for this task |
| `model_id` | `str` | Model/tool to use for execution |
| `task_input` | `Dict[str, Any]` | Input data for the model |
| `metadata` | `Dict[str, Any]` | Metadata for prediction (e.g., image dimensions) |

**Example:**
```json
{
  "task_id": "task-ocr-dec-0",
  "model_id": "easyocr/dec",
  "task_input": {
    "image": "base64_encoded_image_data"
  },
  "metadata": {
    "height": 512,
    "width": 512
  }
}
```

---

### Instance

Represents a compute instance that can execute tasks.

| Field | Type | Description |
|-------|------|-------------|
| `instance_id` | `str` | Unique identifier for this instance |
| `model_id` | `str` | Model running on this instance |
| `endpoint` | `str` | HTTP endpoint for task dispatch |
| `platform_info` | `Dict[str, str]` | Hardware/software information |

**Required `platform_info` fields:**
- `software_name`: Framework/runtime (e.g., "docker", "pytorch")
- `software_version`: Version string (e.g., "20.10", "2.0.1")
- `hardware_name`: Hardware identifier (e.g., "nvidia-rtx-3090")

**Example:**
```json
{
  "instance_id": "instance-gpu-0",
  "model_id": "easyocr/dec",
  "endpoint": "http://localhost:8300",
  "platform_info": {
    "software_name": "docker",
    "software_version": "20.10",
    "hardware_name": "nvidia-rtx-3090"
  }
}
```

---

### TaskStatus

Task lifecycle states.

```python
class TaskStatus(str, Enum):
    PENDING = "pending"      # Task submitted, waiting to be dispatched
    RUNNING = "running"      # Task dispatched to instance, executing
    COMPLETED = "completed"  # Task finished successfully
    FAILED = "failed"        # Task failed with error
```

---

## Queue Information Models

Different scheduling strategies use different queue information models.

### InstanceQueueProbabilistic

Used by **Probabilistic** and **Round Robin** strategies.

| Field | Type | Description |
|-------|------|-------------|
| `instance_id` | `str` | ID of the instance |
| `quantiles` | `List[float]` | Quantile positions (e.g., [0.5, 0.9, 0.95, 0.99]) |
| `values` | `List[float]` | Estimated queue time at each quantile (ms) |

**Interpretation:**
- `quantiles[0] = 0.5, values[0] = 120.5` means 50% chance queue completes in ≤120.5ms
- `quantiles[1] = 0.9, values[1] = 250.3` means 90% chance queue completes in ≤250.3ms

**Example:**
```json
{
  "instance_id": "instance-gpu-0",
  "quantiles": [0.5, 0.9, 0.95, 0.99],
  "values": [120.5, 250.3, 300.7, 450.2]
}
```

---

### InstanceQueueExpectError

Used by **Minimum Time** (Shortest Queue) strategy.

| Field | Type | Description |
|-------|------|-------------|
| `instance_id` | `str` | ID of the instance |
| `expected_time_ms` | `float` | Expected queue completion time (ms) |
| `error_margin_ms` | `float` | Uncertainty/error margin (ms) |

**Interpretation:**
- Queue is expected to complete in `expected_time_ms ± error_margin_ms`
- Example: 150.5 ± 25.3 means queue likely completes between 125.2ms and 175.8ms

**Example:**
```json
{
  "instance_id": "instance-gpu-0",
  "expected_time_ms": 150.5,
  "error_margin_ms": 25.3
}
```

---

## Instance Management Models

### InstanceRegisterRequest

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `instance_id` | `str` | Yes | Unique identifier for the instance |
| `model_id` | `str` | Yes | Model/tool ID running on this instance |
| `endpoint` | `str` | Yes | HTTP endpoint URL of the instance |
| `platform_info` | `Dict[str, str]` | Yes | Platform information for predictions |

### InstanceRegisterResponse

| Field | Type | Description |
|-------|------|-------------|
| `success` | `bool` | Success flag |
| `message` | `str` | Human-readable message |
| `instance` | `Instance` | Registered instance object |

### InstanceRemoveRequest

| Field | Type | Description |
|-------|------|-------------|
| `instance_id` | `str` | ID of the instance to remove |

### InstanceRemoveResponse

| Field | Type | Description |
|-------|------|-------------|
| `success` | `bool` | Success flag |
| `message` | `str` | Human-readable message |
| `instance_id` | `str` | ID of removed instance |

### InstanceListResponse

| Field | Type | Description |
|-------|------|-------------|
| `success` | `bool` | Success flag |
| `count` | `int` | Number of instances returned |
| `instances` | `List[Instance]` | List of instance objects |

### InstanceStats

Statistics for an instance's task execution.

| Field | Type | Description |
|-------|------|-------------|
| `pending_tasks` | `int` | Tasks assigned but not yet dispatched |
| `completed_tasks` | `int` | Successfully completed tasks |
| `failed_tasks` | `int` | Failed tasks |

### InstanceInfoResponse

| Field | Type | Description |
|-------|------|-------------|
| `success` | `bool` | Success flag |
| `instance` | `Instance` | Instance object |
| `queue_info` | `InstanceQueueBase` | Queue info (Probabilistic or ExpectError) |
| `stats` | `InstanceStats` | Execution statistics |

---

## Task Management Models

### TaskSubmitRequest

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `task_id` | `str` | Yes | Unique identifier for the task |
| `model_id` | `str` | Yes | Model/tool ID to use for this task |
| `task_input` | `Dict[str, Any]` | Yes | Input data for the model |
| `metadata` | `Dict[str, Any]` | Yes | Metadata for runtime prediction |

### TaskSubmitResponse

| Field | Type | Description |
|-------|------|-------------|
| `success` | `bool` | Success flag |
| `message` | `str` | Human-readable message |
| `task` | `TaskInfo` | Task information |

### TaskInfo

Basic task information returned after submission.

| Field | Type | Description |
|-------|------|-------------|
| `task_id` | `str` | Task ID |
| `status` | `TaskStatus` | Task status |
| `assigned_instance` | `str` | Instance ID where task was assigned |
| `submitted_at` | `str` | ISO 8601 timestamp |

### TaskListResponse

| Field | Type | Description |
|-------|------|-------------|
| `success` | `bool` | Success flag |
| `count` | `int` | Number of tasks in current page |
| `total` | `int` | Total tasks matching filter |
| `offset` | `int` | Pagination offset |
| `limit` | `int` | Pagination limit |
| `tasks` | `List[TaskSummary]` | List of task summaries |

### TaskSummary

Summary information for task listings.

| Field | Type | Description |
|-------|------|-------------|
| `task_id` | `str` | Task ID |
| `model_id` | `str` | Model ID |
| `status` | `TaskStatus` | Task status |
| `assigned_instance` | `str` | Assigned instance ID |
| `submitted_at` | `str` | ISO 8601 timestamp |
| `completed_at` | `str \| null` | ISO 8601 timestamp (only if completed/failed) |

### TaskTimestamps

| Field | Type | Description |
|-------|------|-------------|
| `submitted_at` | `str` | ISO 8601 timestamp |
| `started_at` | `str \| null` | ISO 8601 timestamp |
| `completed_at` | `str \| null` | ISO 8601 timestamp |

### TaskDetailInfo

Complete task information including input, result, and timestamps.

| Field | Type | Description |
|-------|------|-------------|
| `task_id` | `str` | Task ID |
| `model_id` | `str` | Model ID |
| `status` | `TaskStatus` | Task status |
| `assigned_instance` | `str` | Assigned instance ID |
| `task_input` | `Dict[str, Any]` | Original input data |
| `metadata` | `Dict[str, Any]` | Original metadata |
| `result` | `Dict[str, Any] \| null` | Task output (only if completed) |
| `error` | `str \| null` | Error message (only if failed) |
| `timestamps` | `TaskTimestamps` | Task lifecycle timestamps |
| `execution_time_ms` | `int \| null` | Actual execution time in milliseconds |

### TaskDetailResponse

| Field | Type | Description |
|-------|------|-------------|
| `success` | `bool` | Success flag |
| `task` | `TaskDetailInfo` | Detailed task information |

### TaskClearResponse

| Field | Type | Description |
|-------|------|-------------|
| `success` | `bool` | Success flag |
| `message` | `str` | Human-readable confirmation message |
| `cleared_count` | `int` | Number of tasks that were cleared |

---

## Strategy Management Models

### StrategyInfo

| Field | Type | Description |
|-------|------|-------------|
| `strategy_name` | `str` | Strategy name ("round_robin", "min_time", "probabilistic") |
| `parameters` | `Dict[str, Any]` | Strategy-specific parameters |

**Parameters by strategy:**
- **round_robin**: `{}` (no parameters)
- **min_time**: `{}` (no parameters)
- **probabilistic**: `{"target_quantile": 0.9}` (float, 0.0-1.0)

### StrategySetRequest

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `strategy_name` | `str` | Yes | "round_robin", "min_time", or "probabilistic" |
| `target_quantile` | `float` | No | For probabilistic strategy (default: 0.9) |

### StrategySetResponse

| Field | Type | Description |
|-------|------|-------------|
| `success` | `bool` | Success flag |
| `message` | `str` | Human-readable confirmation message |
| `cleared_tasks` | `int` | Number of tasks removed during strategy switch |
| `reinitialized_instances` | `int` | Number of instances whose queue info was reset |
| `strategy_info` | `StrategyInfo` | New strategy configuration |

---

## Health Check Models

### HealthStats

System-wide statistics.

| Field | Type | Description |
|-------|------|-------------|
| `total_instances` | `int` | Total registered instances |
| `active_instances` | `int` | Currently active instances |
| `total_tasks` | `int` | Total tasks ever submitted |
| `pending_tasks` | `int` | Tasks assigned but not dispatched |
| `running_tasks` | `int` | Tasks currently executing |
| `completed_tasks` | `int` | Successfully completed tasks |
| `failed_tasks` | `int` | Failed tasks |

### HealthResponse

| Field | Type | Description |
|-------|------|-------------|
| `success` | `bool` | Success flag |
| `status` | `str` | "healthy" or "unhealthy" |
| `timestamp` | `str` | ISO 8601 timestamp |
| `version` | `str` | Service version |
| `stats` | `HealthStats` | Service statistics |

---

## WebSocket Models

### WSMessageType

```python
class WSMessageType(str, Enum):
    SUBSCRIBE = "subscribe"        # Subscribe to task result updates
    UNSUBSCRIBE = "unsubscribe"    # Unsubscribe from task updates
    RESULT = "result"              # Task result notification
    ERROR = "error"                # Error notification
    ACK = "ack"                    # Acknowledgment message
```

### WSSubscribeMessage

Client request to subscribe to task results.

| Field | Type | Description |
|-------|------|-------------|
| `type` | `WSMessageType` | "subscribe" |
| `task_ids` | `List[str]` | List of task IDs to subscribe to |

**Example:**
```json
{
  "type": "subscribe",
  "task_ids": ["task-001", "task-002", "task-003"]
}
```

### WSUnsubscribeMessage

Client request to unsubscribe from task results.

| Field | Type | Description |
|-------|------|-------------|
| `type` | `WSMessageType` | "unsubscribe" |
| `task_ids` | `List[str]` | List of task IDs to unsubscribe from |

### WSAckMessage

Server acknowledgment of subscription/unsubscription.

| Field | Type | Description |
|-------|------|-------------|
| `type` | `WSMessageType` | "ack" |
| `message` | `str` | Human-readable confirmation |
| `subscribed_tasks` | `List[str]` | Currently subscribed task IDs |

### WSTaskResultMessage

Server notification of task completion.

| Field | Type | Description |
|-------|------|-------------|
| `type` | `WSMessageType` | "result" |
| `task_id` | `str` | Task ID |
| `status` | `TaskStatus` | "completed" or "failed" |
| `result` | `Dict[str, Any] \| null` | Task output (null if failed) |
| `error` | `str \| null` | Error message (null if completed) |
| `timestamps` | `TaskTimestamps` | Task lifecycle timestamps |
| `execution_time_ms` | `int \| null` | Actual execution time |

**Example (Completed):**
```json
{
  "type": "result",
  "task_id": "task-001",
  "status": "completed",
  "result": {
    "text": "Hello World",
    "confidence": 0.95
  },
  "error": null,
  "timestamps": {
    "submitted_at": "2025-10-31T12:34:56.789Z",
    "started_at": "2025-10-31T12:34:58.123Z",
    "completed_at": "2025-10-31T12:35:01.234Z"
  },
  "execution_time_ms": 3111
}
```

**Example (Failed):**
```json
{
  "type": "result",
  "task_id": "task-002",
  "status": "failed",
  "result": null,
  "error": "Invalid image format: expected JPEG or PNG",
  "timestamps": {
    "submitted_at": "2025-10-31T12:35:10.456Z",
    "started_at": "2025-10-31T12:35:12.789Z",
    "completed_at": "2025-10-31T12:35:13.012Z"
  },
  "execution_time_ms": 223
}
```

### WSErrorMessage

Server error notification.

| Field | Type | Description |
|-------|------|-------------|
| `type` | `WSMessageType` | "error" |
| `error` | `str` | Error description |
| `task_id` | `str \| null` | Task ID if error is task-specific |

---

## Common Response Models

### ErrorResponse

Generic error response format used by all API endpoints.

| Field | Type | Description |
|-------|------|-------------|
| `success` | `bool` | Always `false` |
| `error` | `str` | Error description |

**Example:**
```json
{
  "success": false,
  "error": "Task with this ID already exists"
}
```

---

## Model Validation

All models use [Pydantic](https://docs.pydantic.dev/) for automatic validation:
- **Type checking**: Ensures fields have correct types
- **Required fields**: Missing required fields trigger validation errors
- **Optional fields**: Use `Optional[T]` or `T | null` for optional fields
- **Enums**: String enums ensure only valid values are accepted

## Source Code Reference

All models are defined in `src/model.py`.

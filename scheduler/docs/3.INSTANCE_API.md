# Instance Management API

This document describes the API endpoints for managing compute instances in the scheduler.

> **See Also**: [Data Models](2.DATA_MODELS.md) for request/response model definitions

---

## Overview

Instances are compute resources (e.g., GPU nodes, CPU workers) that execute tasks. Before submitting tasks, you must register at least one instance with the scheduler.

> **⚠️ IMPORTANT: WebSocket Communication**
>
> As of v1.0.0, Instance communication uses **WebSocket** by default for improved performance and reliability.
> - **WebSocket registration** happens automatically when Instance starts
> - **HTTP registration** (below) is maintained for fallback and backward compatibility
> - See [WebSocket Architecture](WEBSOCKET_ARCHITECTURE.md) and [WebSocket API](7.WEBSOCKET_API.md) for details
> - HTTP endpoints documented below are **deprecated** for new deployments

**Key Concepts:**
- Each instance runs a specific model/tool identified by `model_id`
- Instances must provide platform information for runtime prediction
- Queue information is automatically initialized when an instance is registered
- Queue type depends on the configured scheduling strategy
- **WebSocket connections provide persistent bidirectional communication** (recommended)
- HTTP endpoints remain available for fallback scenarios

---

## Endpoints

### POST `/instance/register`

Register a new instance to the scheduler.

**Request Body:**
```json
{
  "instance_id": "instance-gpu-0",
  "model_id": "easyocr/dec",
  "endpoint": "http://192.168.1.100:8300",
  "platform_info": {
    "software_name": "docker",
    "software_version": "20.10",
    "hardware_name": "nvidia-rtx-3090"
  }
}
```

**Field Descriptions:**
- `instance_id`: Unique identifier for this instance (must be unique across all instances)
- `model_id`: Model or tool this instance can execute (e.g., "easyocr/dec", "yolov5")
- `endpoint`: HTTP endpoint where the instance can receive task requests
- `platform_info`: Platform information for runtime prediction
  - `software_name`: Required - Framework/runtime name
  - `software_version`: Required - Version string
  - `hardware_name`: Required - Hardware identifier

**Success Response (200 OK):**
```json
{
  "success": true,
  "message": "Instance registered successfully",
  "instance": {
    "instance_id": "instance-gpu-0",
    "model_id": "easyocr/dec",
    "endpoint": "http://192.168.1.100:8300",
    "platform_info": {
      "software_name": "docker",
      "software_version": "20.10",
      "hardware_name": "nvidia-rtx-3090"
    }
  }
}
```

**Error Responses:**

**400 Bad Request** - Instance already exists:
```json
{
  "success": false,
  "error": "Instance with this ID already exists"
}
```

**400 Bad Request** - Missing required platform_info fields:
```json
{
  "success": false,
  "error": "platform_info missing required keys: {'software_name', 'hardware_name'}"
}
```

**Example:**
```bash
curl -X POST http://localhost:8000/instance/register \
  -H "Content-Type: application/json" \
  -d '{
    "instance_id": "instance-gpu-0",
    "model_id": "easyocr/dec",
    "endpoint": "http://192.168.1.100:8300",
    "platform_info": {
      "software_name": "docker",
      "software_version": "20.10",
      "hardware_name": "nvidia-rtx-3090"
    }
  }'
```

---

### POST `/instance/remove`

Safely remove an instance from the scheduler.

**Description:**

This endpoint permanently removes an instance from the scheduler's registry. For safe removal, the instance **must be in DRAINING state** with **no pending tasks**.

**⚠️ Safe Removal Requirements:**
1. Instance must be in `DRAINING` state (use `/instance/drain` first)
2. Instance must have **0 pending tasks** (check with `/instance/drain/status`)

If these conditions are not met, the request will be rejected with a 400 error.

**Request Body:**
```json
{
  "instance_id": "instance-gpu-0"
}
```

**Field Descriptions:**
- `instance_id`: ID of the instance to remove

**Success Response (200 OK):**
```json
{
  "success": true,
  "message": "Instance removed successfully",
  "instance_id": "instance-gpu-0"
}
```

**Error Responses:**

**404 Not Found** - Instance does not exist:
```json
{
  "success": false,
  "error": "Instance not found"
}
```

**400 Bad Request** - Instance is not in DRAINING state or has pending tasks:
```json
{
  "success": false,
  "error": "Cannot remove instance: Instance must be in DRAINING state with no pending tasks",
  "hint": "Use /instance/drain first and wait for tasks to complete"
}
```

**Recommended Workflow:**

```bash
# Step 1: Drain the instance (stops new task assignments)
curl -X POST http://localhost:8000/instance/drain \
  -H "Content-Type: application/json" \
  -d '{"instance_id": "instance-gpu-0"}'

# Step 2: Wait for tasks to complete
while true; do
  CAN_REMOVE=$(curl -s "http://localhost:8000/instance/drain/status?instance_id=instance-gpu-0" | jq -r '.can_remove')
  if [ "$CAN_REMOVE" = "true" ]; then
    break
  fi
  echo "Waiting for tasks to complete..."
  sleep 5
done

# Step 3: Remove the instance
curl -X POST http://localhost:8000/instance/remove \
  -H "Content-Type: application/json" \
  -d '{"instance_id": "instance-gpu-0"}'
```

**Example:**
```bash
curl -X POST http://localhost:8000/instance/remove \
  -H "Content-Type: application/json" \
  -d '{
    "instance_id": "instance-gpu-0"
  }'
```

**What Happens After Removal:**
- Instance is permanently removed from the registry
- Instance will no longer appear in `/instance/list`
- Instance queue info and statistics are deleted
- Any future tasks for this model will be assigned to other instances
- **Note**: Currently, pending tasks are **not** automatically reassigned to other instances

---

### POST `/instance/drain`

Start draining an instance - stop assigning new tasks.

**Description:**

The drain operation prepares an instance for safe removal. When an instance is draining:
- It will **not** receive any new task assignments
- Existing tasks in its queue continue to execute normally
- The instance remains registered and visible in `/instance/list`

This is the **recommended first step** before removing an instance from the scheduler.

**Request Body:**
```json
{
  "instance_id": "instance-gpu-0"
}
```

**Field Descriptions:**
- `instance_id`: ID of the instance to drain

**Success Response (200 OK):**
```json
{
  "success": true,
  "message": "Instance is now draining. No new tasks will be assigned.",
  "instance_id": "instance-gpu-0",
  "status": "draining",
  "pending_tasks": 5,
  "running_tasks": 0,
  "estimated_completion_time_ms": 1250.5
}
```

**Response Fields:**
- `message`: Human-readable confirmation message
- `instance_id`: ID of the draining instance
- `status`: Current instance status (always `"draining"` after this call)
- `pending_tasks`: Number of tasks still in the instance's queue
- `running_tasks`: Number of tasks currently executing (currently always 0)
- `estimated_completion_time_ms`: Estimated time until queue is empty (nullable)
  - Based on queue info from the current scheduling strategy
  - `null` if estimation is unavailable

**Error Responses:**

**404 Not Found** - Instance does not exist:
```json
{
  "success": false,
  "error": "Instance not found"
}
```

**400 Bad Request** - Instance is not in ACTIVE state:
```json
{
  "success": false,
  "error": "Instance must be in ACTIVE state to drain"
}
```

**Example:**
```bash
curl -X POST http://localhost:8000/instance/drain \
  -H "Content-Type: application/json" \
  -d '{
    "instance_id": "instance-gpu-0"
  }'
```

**Workflow for Safe Instance Removal:**

```bash
# Step 1: Start draining the instance
curl -X POST http://localhost:8000/instance/drain \
  -H "Content-Type: application/json" \
  -d '{"instance_id": "instance-gpu-0"}'

# Step 2: Monitor drain status
while true; do
  STATUS=$(curl -s "http://localhost:8000/instance/drain/status?instance_id=instance-gpu-0" | jq -r '.can_remove')
  if [ "$STATUS" = "true" ]; then
    echo "Instance is ready for removal"
    break
  fi
  echo "Waiting for tasks to complete..."
  sleep 5
done

# Step 3: Remove the instance
curl -X POST http://localhost:8000/instance/remove \
  -H "Content-Type: application/json" \
  -d '{"instance_id": "instance-gpu-0"}'
```

---

### GET `/instance/drain/status`

Check the draining status of an instance.

**Description:**

Use this endpoint to monitor the progress of a draining instance and determine when it's safe to remove. The endpoint returns whether the instance has finished processing all pending tasks.

**Query Parameters:**
- `instance_id` (required, string): ID of the instance to check

**Example Request:**
```
GET /instance/drain/status?instance_id=instance-gpu-0
```

**Success Response (200 OK):**

**While draining (tasks remaining):**
```json
{
  "success": true,
  "instance_id": "instance-gpu-0",
  "status": "draining",
  "pending_tasks": 3,
  "running_tasks": 0,
  "can_remove": false,
  "drain_initiated_at": "2025-10-31T12:34:56.789Z"
}
```

**Ready for removal (no tasks):**
```json
{
  "success": true,
  "instance_id": "instance-gpu-0",
  "status": "draining",
  "pending_tasks": 0,
  "running_tasks": 0,
  "can_remove": true,
  "drain_initiated_at": "2025-10-31T12:34:56.789Z"
}
```

**Response Fields:**
- `instance_id`: ID of the queried instance
- `status`: Current instance status
  - `"draining"` - Instance is in draining mode
  - `"active"` - Instance is not draining (drain not started)
- `pending_tasks`: Number of tasks still in queue
- `running_tasks`: Number of tasks currently executing (currently always 0)
- `can_remove`: Boolean indicating if it's safe to remove the instance
  - `true` if status is `"draining"` AND `pending_tasks` is 0
  - `false` otherwise
- `drain_initiated_at`: ISO 8601 timestamp when draining started (nullable)
  - `null` if instance is not draining

**Error Response (404 Not Found):**
```json
{
  "success": false,
  "error": "Instance not found"
}
```

**Example:**
```bash
# Check drain status
curl "http://localhost:8000/instance/drain/status?instance_id=instance-gpu-0"

# Pretty-print with jq
curl "http://localhost:8000/instance/drain/status?instance_id=instance-gpu-0" | jq

# Extract can_remove flag
curl -s "http://localhost:8000/instance/drain/status?instance_id=instance-gpu-0" | jq -r '.can_remove'
```

**Polling for Safe Removal:**
```bash
# Bash script to wait for safe removal
INSTANCE_ID="instance-gpu-0"

while true; do
  RESPONSE=$(curl -s "http://localhost:8000/instance/drain/status?instance_id=$INSTANCE_ID")
  CAN_REMOVE=$(echo $RESPONSE | jq -r '.can_remove')
  PENDING=$(echo $RESPONSE | jq -r '.pending_tasks')

  if [ "$CAN_REMOVE" = "true" ]; then
    echo "✓ Instance is ready for removal"
    break
  else
    echo "Waiting... ($PENDING tasks remaining)"
    sleep 5
  fi
done
```

**Python Example:**
```python
import requests
import time

def wait_for_drain_completion(instance_id: str, poll_interval: int = 5):
    """Wait until instance is ready for removal."""
    url = f"http://localhost:8000/instance/drain/status"
    params = {"instance_id": instance_id}

    while True:
        response = requests.get(url, params=params)
        data = response.json()

        if data.get("can_remove"):
            print(f"✓ Instance {instance_id} is ready for removal")
            return True

        pending = data.get("pending_tasks", 0)
        print(f"Waiting... ({pending} tasks remaining)")
        time.sleep(poll_interval)

# Usage
wait_for_drain_completion("instance-gpu-0")
```

---

### GET `/instance/list`

List all instances currently registered in the scheduler.

**Query Parameters:**
- `model_id` (optional, string): Filter instances by model ID

**Example Requests:**
```
GET /instance/list
GET /instance/list?model_id=easyocr/dec
```

**Success Response (200 OK):**
```json
{
  "success": true,
  "count": 2,
  "instances": [
    {
      "instance_id": "instance-gpu-0",
      "model_id": "easyocr/dec",
      "endpoint": "http://192.168.1.100:8300",
      "platform_info": {
        "software_name": "docker",
        "software_version": "20.10",
        "hardware_name": "nvidia-rtx-3090"
      }
    },
    {
      "instance_id": "instance-gpu-1",
      "model_id": "easyocr/dec",
      "endpoint": "http://192.168.1.101:8300",
      "platform_info": {
        "software_name": "docker",
        "software_version": "20.10",
        "hardware_name": "nvidia-rtx-3080"
      }
    }
  ]
}
```

**Response Fields:**
- `count`: Number of instances returned (after filtering)
- `instances`: Array of instance objects

**Example:**
```bash
# List all instances
curl "http://localhost:8000/instance/list"

# List instances for specific model
curl "http://localhost:8000/instance/list?model_id=easyocr/dec"
```

---

### GET `/instance/info`

Get detailed information about a specific instance, including queue state and statistics.

**Query Parameters:**
- `instance_id` (required, string): ID of the instance to query

**Example Request:**
```
GET /instance/info?instance_id=instance-gpu-0
```

**Success Response (200 OK):**
```json
{
  "success": true,
  "instance": {
    "instance_id": "instance-gpu-0",
    "model_id": "easyocr/dec",
    "endpoint": "http://192.168.1.100:8300",
    "platform_info": {
      "software_name": "docker",
      "software_version": "20.10",
      "hardware_name": "nvidia-rtx-3090"
    }
  },
  "queue_info": {
    "instance_id": "instance-gpu-0",
    "quantiles": [0.5, 0.9, 0.95, 0.99],
    "values": [120.5, 250.3, 300.8, 450.2]
  },
  "stats": {
    "pending_tasks": 5,
    "completed_tasks": 120,
    "failed_tasks": 2
  }
}
```

**Response Fields:**
- `instance`: Instance configuration
- `queue_info`: Current queue state (format depends on scheduling strategy)
  - **Probabilistic strategy**: `InstanceQueueProbabilistic` with `quantiles` and `values`
  - **Shortest Queue strategy**: `InstanceQueueExpectError` with `expected_time_ms` and `error_margin_ms`
- `stats`: Execution statistics
  - `pending_tasks`: Tasks assigned but not yet dispatched
  - `completed_tasks`: Successfully completed tasks
  - `failed_tasks`: Tasks that failed

**Error Response (404 Not Found):**
```json
{
  "success": false,
  "error": "Instance not found"
}
```

**Example:**
```bash
curl "http://localhost:8000/instance/info?instance_id=instance-gpu-0"
```

---

## Queue Information by Strategy

The `queue_info` field in the `/instance/info` response varies by scheduling strategy:

### Probabilistic Strategy

```json
{
  "instance_id": "instance-gpu-0",
  "quantiles": [0.5, 0.9, 0.95, 0.99],
  "values": [120.5, 250.3, 300.8, 450.2]
}
```

- `quantiles`: Percentile positions (e.g., 0.5 = 50th percentile = median)
- `values`: Estimated queue completion time (ms) at each quantile

**Interpretation**: There's a 50% chance the queue will complete in ≤120.5ms, 90% chance in ≤250.3ms, etc.

### Shortest Queue Strategy

```json
{
  "instance_id": "instance-gpu-0",
  "expected_time_ms": 150.5,
  "error_margin_ms": 25.3
}
```

- `expected_time_ms`: Expected queue completion time (mean)
- `error_margin_ms`: Uncertainty/error margin (standard deviation)

**Interpretation**: Queue is expected to complete in 150.5ms ± 25.3ms.

---

## Common Use Cases

### Registering Multiple Instances

To distribute load across multiple GPUs:

```bash
# Register GPU 0
curl -X POST http://localhost:8000/instance/register \
  -H "Content-Type: application/json" \
  -d '{
    "instance_id": "instance-gpu-0",
    "model_id": "easyocr/dec",
    "endpoint": "http://192.168.1.100:8300",
    "platform_info": {
      "software_name": "docker",
      "software_version": "20.10",
      "hardware_name": "nvidia-rtx-3090"
    }
  }'

# Register GPU 1
curl -X POST http://localhost:8000/instance/register \
  -H "Content-Type: application/json" \
  -d '{
    "instance_id": "instance-gpu-1",
    "model_id": "easyocr/dec",
    "endpoint": "http://192.168.1.101:8300",
    "platform_info": {
      "software_name": "docker",
      "software_version": "20.10",
      "hardware_name": "nvidia-rtx-3080"
    }
  }'
```

### Monitoring Instance Load

Check queue state to identify overloaded instances:

```bash
curl "http://localhost:8000/instance/info?instance_id=instance-gpu-0"
```

If `pending_tasks` is high or `expected_time_ms` is large, consider adding more instances.

### Checking Instance Statistics

```bash
# Get all instances
curl "http://localhost:8000/instance/list" | jq '.instances[] | {id: .instance_id, model: .model_id}'

# Check each instance's stats
curl "http://localhost:8000/instance/info?instance_id=instance-gpu-0" | jq '.stats'
```

---

## Platform Information Best Practices

### Consistent Naming

Use consistent identifiers across instances for better prediction accuracy:

**Good:**
```json
{
  "software_name": "docker",
  "software_version": "20.10",
  "hardware_name": "nvidia-rtx-3090"
}
```

**Avoid:**
```json
{
  "software_name": "Docker",  // Inconsistent casing
  "software_version": "20.10.0",  // Inconsistent version format
  "hardware_name": "RTX 3090"  // Inconsistent naming
}
```

### Hardware Naming Convention

Recommended format: `vendor-model` (lowercase, hyphen-separated)

Examples:
- `nvidia-rtx-3090`
- `nvidia-a100`
- `amd-mi100`
- `cpu-xeon-platinum-8280`

### Software Naming Convention

Use the primary framework/runtime identifier:

Examples:
- `docker` - For containerized workloads
- `pytorch` - For PyTorch-based models
- `tensorflow` - For TensorFlow-based models
- `onnx` - For ONNX runtime

---

## Error Handling

All endpoints follow a consistent error response format:

```json
{
  "success": false,
  "error": "Error description here"
}
```

**Common HTTP Status Codes:**
- **200 OK**: Request succeeded
- **400 Bad Request**: Invalid request data (e.g., missing fields, duplicate instance_id)
- **404 Not Found**: Instance not found
- **422 Unprocessable Entity**: Validation error (e.g., invalid model format)

---

## Related Documentation

- [Data Models](2.DATA_MODELS.md) - Model definitions
- [Task Management API](4.TASK_API.md) - Submitting tasks to instances
- [Scheduling Strategies](8.SCHEDULING_STRATEGIES.md) - How instances are selected for tasks

# Scheduler API Reference

## Overview

The Scheduler service manages task distribution across compute instances using configurable scheduling strategies. It handles task submission, instance registration, and provides real-time task result notifications.

**Key Capabilities:**
- Multi-strategy task scheduling (round-robin, minimum time, probabilistic)
- Real-time task result delivery via WebSocket
- Dynamic queue management with runtime prediction
- Instance registration and monitoring
- Task lifecycle tracking and statistics

---

## Documentation Structure

This documentation is organized into focused modules for clarity and maintainability:

### Core Concepts

1. **[Data Models](2.DATA_MODELS.md)**
   - Core models (Task, Instance, TaskStatus)
   - Queue information models (Probabilistic, ExpectError)
   - Request/Response models for all APIs
   - WebSocket message models

### API Endpoints

2. **[Instance Management API](3.INSTANCE_API.md)**
   - Register and remove instances
   - List and query instances
   - Platform information requirements
   - Queue information by strategy

3. **[Task Management API](4.TASK_API.md)**
   - Submit and track tasks
   - Query task status and results
   - Task lifecycle and filtering
   - Clear tasks (maintenance)

4. **[Strategy Management API](5.STRATEGY_API.md)**
   - Query current strategy
   - Switch between strategies
   - Strategy-specific parameters
   - Safe strategy switching workflow

5. **[Health Check API](6.HEALTH_API.md)**
   - Service health monitoring
   - Statistics and metrics
   - Load balancer integration
   - Kubernetes probes

6. **[WebSocket API](7.WEBSOCKET_API.md)**
   - Real-time task result notifications
   - Subscription patterns
   - Message types and examples
   - Connection management

### Advanced Topics

7. **[Scheduling Strategies](8.SCHEDULING_STRATEGIES.md)**
   - Round Robin algorithm
   - Minimum Time (Shortest Queue) algorithm
   - Probabilistic algorithm
   - Queue update mechanisms
   - Strategy comparison and selection guide

8. **[Usage Examples](9.USAGE_EXAMPLES.md)**
   - Complete Python workflow with WebSocket
   - Batch task processing
   - Multi-model workflows
   - Strategy benchmarking
   - Error handling and retry patterns

---

## Quick Start

### 1. Register an Instance

```bash
curl -X POST http://localhost:8000/instance/register \
  -H "Content-Type: application/json" \
  -d '{
    "instance_id": "instance-gpu-0",
    "model_id": "easyocr/dec",
    "endpoint": "http://192.168.1.100:8300",
    "platform_info": {
      "software_name": "docker",
      "software_version": "20.10",
      "hardware_name": "nvidia-rtx-3090"
    }
  }'
```

### 2. Submit a Task

```bash
curl -X POST http://localhost:8000/task/submit \
  -H "Content-Type: application/json" \
  -d '{
    "task_id": "task-001",
    "model_id": "easyocr/dec",
    "task_input": {
      "image": "base64_encoded_image_data"
    },
    "metadata": {
      "height": 512,
      "width": 512
    }
  }'
```

### 3. Query Task Status

```bash
curl "http://localhost:8000/task/info?task_id=task-001"
```

### 4. Check Service Health

```bash
curl "http://localhost:8000/health"
```

---

## API Endpoints Summary

### Instance Management

| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | `/instance/register` | Register a new instance |
| POST | `/instance/remove` | Remove an instance |
| GET | `/instance/list` | List all instances (optionally filter by model) |
| GET | `/instance/info` | Get detailed instance information and queue state |

### Task Management

| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | `/task/submit` | Submit a new task for execution |
| GET | `/task/list` | List tasks with filtering and pagination |
| GET | `/task/info` | Get detailed task information |
| POST | `/task/clear` | Clear all tasks from registry |

### Strategy Management

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/strategy/get` | Get current scheduling strategy |
| POST | `/strategy/set` | Set scheduling strategy |

### Health Check

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/health` | Check service health and statistics |

### WebSocket

| Endpoint | Protocol | Description |
|----------|----------|-------------|
| `/task/get_result` | WebSocket | Real-time task result notifications |

---

## Key Concepts

### Task Lifecycle

1. **PENDING** - Task submitted, scheduler selecting instance
2. **RUNNING** - Task dispatched to instance, executing
3. **COMPLETED** - Task finished successfully with result
4. **FAILED** - Task failed with error message

### Scheduling Strategies

- **Round Robin**: Simple cyclic distribution (no predictor required)
- **Minimum Time**: Greedy selection based on expected queue time
- **Probabilistic**: Quantile-based selection for tail latency optimization

See [Scheduling Strategies](8.SCHEDULING_STRATEGIES.md) for detailed explanations.

### Queue Information

Different strategies use different queue information models:

- **Probabilistic/Round Robin**: `InstanceQueueProbabilistic` (quantiles + values)
- **Minimum Time**: `InstanceQueueExpectError` (expected_time_ms + error_margin_ms)

See [Data Models](2.DATA_MODELS.md) for detailed field descriptions.

---

## Common Workflows

### Basic Task Submission

1. Register instance(s) → [Instance API](3.INSTANCE_API.md#post-instanceregister)
2. Submit task → [Task API](4.TASK_API.md#post-tasksubmit)
3. Poll task status → [Task API](4.TASK_API.md#get-taskinfo)

### Real-Time Task Monitoring (Recommended)

1. Register instance(s) → [Instance API](3.INSTANCE_API.md#post-instanceregister)
2. Connect to WebSocket → [WebSocket API](7.WEBSOCKET_API.md#connection)
3. Subscribe to task IDs → [WebSocket API](7.WEBSOCKET_API.md#subscribe-message)
4. Submit tasks → [Task API](4.TASK_API.md#post-tasksubmit)
5. Receive results via WebSocket → [WebSocket API](7.WEBSOCKET_API.md#task-result-message)

### Strategy Optimization

1. Check current strategy → [Strategy API](5.STRATEGY_API.md#get-strategyget)
2. Clear running tasks → [Task API](4.TASK_API.md#post-taskclear)
3. Switch strategy → [Strategy API](5.STRATEGY_API.md#post-strategyset)
4. Submit benchmark workload
5. Compare results

See [Usage Examples](9.USAGE_EXAMPLES.md) for complete code examples.

---

## Error Handling

All endpoints follow a consistent error response format:

```json
{
  "success": false,
  "error": "Error description here"
}
```

**Common HTTP Status Codes:**
- **200 OK**: Request succeeded
- **400 Bad Request**: Invalid request data
- **404 Not Found**: Resource not found
- **503 Service Unavailable**: Service error (e.g., predictor unavailable)
- **422 Unprocessable Entity**: Validation error

---

## Performance Considerations

### Task Submission Latency

- Validation: < 1ms
- Instance lookup: < 1ms
- **Predictor API call**: 10-100ms (depends on network/predictor load)
- Strategy selection: < 1ms
- Queue update: < 1ms

**Optimization**: Enable predictor caching to reduce latency.

### Result Retrieval

| Method | Latency | Efficiency | Use Case |
|--------|---------|------------|----------|
| Polling `/task/info` | 100-500ms avg | Low | Simple scripts, rare tasks |
| WebSocket subscription | < 10ms | High | Production, high throughput |

**Recommendation**: Always use WebSocket for production workloads.

---

## System Requirements

- Python 3.10+
- FastAPI web framework
- Access to predictor service (for min_time and probabilistic strategies)
- WebSocket support for real-time notifications

---

## Additional Resources

- [Main Scheduler README](../README.md) - Installation and configuration
- [Predictor Documentation](../../predictor/docs/1.API_REFERENCE.md) - Predictor service integration

---

## Support and Contribution

For issues, feature requests, or contributions, please refer to the main project repository.

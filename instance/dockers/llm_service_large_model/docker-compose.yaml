services:
  llm_service_large_model:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ${CONTAINER_NAME:-llm_service_large_model}
    ports:
      - "${MODEL_PORT:-8000}:8000"
    environment:
      - MODEL_ID=${MODEL_ID:-llm_service_large_model}
      - INSTANCE_ID=${INSTANCE_ID:-instance-default}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - MODEL_MODEL_PATH=${MODEL_MODEL_PATH}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s


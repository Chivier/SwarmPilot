[project]
name = "llm-service-large-model"
version = "0.1.0"
description = "LLM service container with sglang model loading"
requires-python = ">=3.11"
dependencies = [
    "accelerate>=1.11.0",
    "diffusers>=0.35.2",
    "fastapi",
    "ftfy>=6.3.1",
    "sglang",
    "uvicorn[standard]",
]

# Instance Service Architecture

## Overview

The Instance Service is the minimal execution unit and scheduling unit in the SwarmPilot project. Each instance serves a single model at a time and provides task queue management for processing inference requests sequentially.

## Core Features

### 1. Task Queue Management
- Accept multiple tasks simultaneously
- Process tasks sequentially in FIFO (First-In-First-Out) order
- Track task status throughout execution lifecycle
- Support task cancellation for queued tasks

### 2. Dynamic Model Management
- Each instance serves only one model/tool at a time
- Support hot-swapping between different models via API
- Docker-based model isolation and resource management
- Automatic model container lifecycle management

## Architecture Components

### Instance Core
The main service that:
- Exposes REST API endpoints for management and task submission
- Manages the task queue and execution flow
- Handles model container lifecycle (start/stop)
- Maintains instance state and health status
- Coordinates WebSocket communication with Scheduler

### Model Container
Docker containers that:
- Run specific ML models or processing tools
- Expose standardized `/inference` endpoint
- Use uv-based Python environment management
- Handle graceful shutdown and resource cleanup

### Task Queue
In-memory or persistent queue that:
- Stores submitted tasks in FIFO order
- Tracks task states: `queued`, `running`, `completed`, `failed`
- Provides task query and management capabilities
- Sends task results via WebSocket or HTTP callback

### WebSocket Client
Bidirectional communication channel that:
- Establishes persistent connection to Scheduler
- Receives real-time task submissions from Scheduler
- Sends task results and status updates
- Implements automatic reconnection with exponential backoff
- Provides heartbeat mechanism for connection health
- Falls back to HTTP if connection fails

## Task Definition

```python
class Task(BaseModel):
    task_id: str              # Unique identifier for this task
    model_id: str             # Model/tool ID to use for this task
    task_input: Dict[str, Any]  # Model-specific input data
```

## Task Lifecycle

```
┌─────────────┐
│  Submitted  │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   Queued    │ ◄── Task waiting in queue
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   Running   │ ◄── Task being processed by model
└──────┬──────┘
       │
       ├─────────────┐
       ▼             ▼
┌─────────────┐ ┌─────────────┐
│  Completed  │ │   Failed    │
└─────────────┘ └─────────────┘
```

## Port Allocation Strategy

The instance uses a predictable port allocation scheme:

- **Instance API**: `<instance_port>` (e.g., 5000)
- **Model Container**: `<instance_port> + 1000` (e.g., 6000)

This allows:
- Multiple instances to run on the same host
- Clear port separation between instance and model services
- Easy proxy routing from instance to model

## Instance States

| State | Description |
|-------|-------------|
| `idle` | Instance running, no model loaded |
| `running` | Model loaded and accepting tasks |
| `busy` | Model processing a task |
| `error` | Instance or model in error state |

## Communication Architecture

### WebSocket-Based Communication

The Instance Service uses WebSocket for real-time bidirectional communication with the Scheduler, providing significant performance improvements over HTTP polling.

#### Connection Lifecycle

```
┌─────────────────────────────────────────────────────────────┐
│                    Connection Lifecycle                      │
└─────────────────────────────────────────────────────────────┘

POST /model/start
       │
       ├──> 1. Start model container
       │
       ├──> 2. Establish WebSocket connection to Scheduler
       │
       ├──> 3. Send REGISTER message
       │         - instance_id
       │         - model_id
       │         - platform_info
       │
       ├──> 4. Begin message loop
       │         - Listen for TASK_SUBMIT
       │         - Send TASK_RESULT
       │         - Exchange HEARTBEAT
       │
       └──> 5. Maintain connection until model stop
                  - Auto-reconnect on disconnect
                  - Exponential backoff retry
```

#### Message Flow

**Task Submission:**
```
Scheduler                    Instance
    │                            │
    ├─── TASK_SUBMIT ────────>   │  (task_id, model_id, task_input)
    │                            ├─── Queue task
    │   <──── TASK_ACK ──────────┤  (success, queue_position)
    │                            │
    │                            ├─── Execute task
    │                            │
    │   <─── TASK_RESULT ────────┤  (task_id, status, result, execution_time)
    │                            │
```

**Heartbeat Mechanism:**
```
Instance                    Scheduler
    │                            │
    ├──── HEARTBEAT ──────────>  │  (every 30 seconds)
    │   <──── HEARTBEAT ─────────┤
    │                            │
    │   (connection healthy)     │
```

#### HTTP Fallback

If WebSocket connection is unavailable:
1. Task results are sent via HTTP callback to `/callback/task_result`
2. Instance registration uses HTTP endpoint `/instance/register`
3. Instance continues normal operation with higher latency

### Dual-Protocol Support

The Instance Service supports both protocols:

| Feature | WebSocket | HTTP |
|---------|-----------|------|
| **Latency** | 5-8x lower | Baseline |
| **Task Submission** | Real-time push | Polling/API call |
| **Connection** | Persistent | Request/response |
| **Reconnection** | Automatic | Manual |
| **Use Case** | Production (default) | Development/fallback |

## Scaling Considerations

### Horizontal Scaling
- Deploy multiple instances with different `instance_id`
- Each instance operates independently
- External scheduler (e.g., SwarmPilot Scheduler) distributes tasks

### Resource Isolation
- Each model container has dedicated resource limits (CPU, memory, GPU)
- Docker provides process isolation and resource constraints
- Prevents resource contention between models

## Next Steps

- [Model Container Specification](./2.MODEL_CONTAINER_SPEC.md) - Learn how to create compliant model containers
- [Model Registry Guide](./3.MODEL_REGISTRY.md) - Configure available models
- [API Reference](./5.API_REFERENCE.md) - Integrate with the instance API

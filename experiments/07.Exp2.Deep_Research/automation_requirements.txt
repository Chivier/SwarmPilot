# Optional dependencies for automation script
# Install with: pip install -r automation_requirements.txt
# Or with uv: cat automation_requirements.txt | xargs -n 1 uv add

# Required dependencies
pyyaml>=6.0
requests>=2.31.0

# Optional dependencies for visualization
matplotlib>=3.7.0
numpy>=1.24.0
